import { ParentDashboard as ParentDashboardFeature } from '../../features/communications/components/ParentDashboard'

export default function ParentDashboard({ user }) {
  return <ParentDashboardFeature user={user} />
}
